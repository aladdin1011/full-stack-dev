mkdir — создаёт каталоги.
echo — добавляет текст в файл.
touch — создаёт пустой файл.
mv — перемещает файл.
ls -R — отображает структуру каталога рекурсивно.
grep — находит строки с определённым текстом.
wc -l — считает строки в файле.
find — ищет файлы по заданному шаблону.
